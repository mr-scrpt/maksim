<section class="section">
	<div class="section__box layout__box {{inject.pageSectionClass}}">
		{% if inject.title %}
			<div class="section__title title">
				<div class="title__main">{{inject.title}}</div>
			</div>
		{% endif %}

		{% block content %}{% endblock %}
	</div>
</section>