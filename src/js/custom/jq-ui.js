/* $(document).ready(() => {
    const width = $(window).width();
    const collapsible = width > 540 ? false : true;
    $(".qa-item__inner").accordion({
        active: false,
        heightStyle: "content",
        collapsible: true,
    });

    $(".menu-slide__inner").accordion({
        active: false,
        heightStyle: "content",
        collapsible,
    });

    $(".footer__box_slide .footer-cell__item").accordion({
        active: false,
        heightStyle: "content",
        collapsible,
    });
});
 */